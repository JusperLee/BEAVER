{
  "id": "beaver-compression",
  "name": "BEAVER Prompt Compression",
  "description": "Compress long text context using BEAVER hierarchical prompt compression before sending to LLM. Reduces token usage by up to 26x while preserving key information. All compression parameters are auto-detected by the server.",
  "configSchema": {
    "type": "object",
    "properties": {
      "serverUrl": {
        "type": "string",
        "default": "http://127.0.0.1:8765",
        "description": "URL of the BEAVER compression API server"
      }
    }
  }
}
